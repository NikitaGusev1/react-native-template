import { DefaultTheme } from 'styled-components';

const defaultTheme: DefaultTheme = {
  borderRadius: '5px',

  colors: {
    main: 'cyan',
    secondary: 'magenta',
    text: 'black',
    background: 'white',
  },

  sizes: {
    appPadding: 16,
  },
};

export { defaultTheme };
